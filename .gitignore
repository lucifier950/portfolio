# Dependencies — re-installed via `npm install`, never committed (huge!)
node_modules

# Build output — regenerated by `npm run build`
dist
dist-ssr

# Logs
*.log
npm-debug.log*

# Editor / OS files
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Local environment variables (secrets)
.env
.env.local
.env.*.local
.vercel
